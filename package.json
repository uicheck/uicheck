{
  "name": "uicheck",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r run build",
    "dev:web": "pnpm --filter @uicheck/web run dev",
    "dev:mcp": "pnpm --filter @uicheck/mcp run start",
    "dev:site": "pnpm --filter @uicheck/site run dev",
    "lint": "pnpm --filter @uicheck/site run lint",
    "prepare:workspace": "pnpm --filter @uicheck/core run build && pnpm --filter @uicheck/mcp run build",
    "test": "pnpm run prepare:workspace && pnpm -r run test",
    "test:evidence": "pnpm run test:evidence:taro-weapp",
    "test:evidence:taro-weapp": "pnpm run prepare:workspace && pnpm --filter @uicheck/taro run build && pnpm --filter taro-demo run typecheck && pnpm --filter taro-demo run build:weapp && pnpm --filter @uicheck/taro run test:weapp-snapshot",
    "typecheck": "pnpm run prepare:workspace && pnpm -r run typecheck"
  },
  "pnpm": {
    "overrides": {
      "postcss": "^8.5.10"
    }
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "miniprogram-automator": "^0.12.1",
    "playwright": "^1.60.0",
    "ws": "^8.20.1"
  }
}

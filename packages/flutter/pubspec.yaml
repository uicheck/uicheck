name: uicheck_flutter
description: Flutter runtime client for UI Check MCP inspection.
version: 0.1.7
homepage: https://uicheck.ai
repository: https://github.com/uicheck/uicheck

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
